# PyTest Aardvark Fixtures

This simple PyTest plugin saves fixtures to ASDF files/experiments when
annotated with `@save_fixture`. This is used for compatibility tests between
python version and different dependencies.
